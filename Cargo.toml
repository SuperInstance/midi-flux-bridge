[package]
name = "midi-flux-bridge"
version = "0.1.0"
edition = "2021"
description = "Bridge between MIDI events and flux-based state propagation in musical agent networks"
license = "MIT"
repository = "https://github.com/SuperInstance/midi-flux-bridge"
keywords = ["midi", "flux", "bridge", "music", "agents"]

[dependencies]
serde = { version = "1", features = ["derive"] }
